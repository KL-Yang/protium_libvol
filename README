create a very simple protium volume format dataset.

fixed length header, optional rectangle map,
seismic attributes.

This is used for debug, and development of protium_viewer.

later can try to use python object interface:
https://docs.python.org/3/extending/newtypes_tutorial.html#defining-new-types

volslice.py: create slice from vol as a new vol
